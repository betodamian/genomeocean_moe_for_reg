# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv/
venv/
env/

# OS
.DS_Store

# JetBrains
.idea/

# Slide build scaffolding
slides_build/

# Pulled data — fetched via committed scripts (data/download/*.sh), pinned by
# data/download/checksums.txt (research_plan.md §14). Kept locally in the project,
# not committed, to keep the repo lean and reproducible. Regenerate with the scripts.
data/genomes/
data/promoters/PPD/
data/promoters/PPD_csv.zip
data/**/raw/
data/download/_tmp/
*.zip
*.tar.gz
*.bam
*.sra
*.bw
*.bigwig

# Generated datasets (windows/decoys) — regenerable via pipeline/
data/datasets/

# API keys and credentials
cborg_claude_api_key
*.api_key
*.secret

# Large feature caches — regenerable via pipeline/submit_phase0.sh
*.npz
